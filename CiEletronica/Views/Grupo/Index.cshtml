@model IEnumerable<CiEletronica.Models.gru_grupo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.gru_nom_grupo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.gru_nom_grupo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.gru_id_gru }) |
            @Html.ActionLink("Details", "Details", new { id=item.gru_id_gru }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.gru_id_gru })
        </td>
    </tr>
}

</table>
